/**
 * Re-export the session hook from context for convenience.
 * This file exists to maintain the hooks/ directory convention from the spec.
 */

export { useSession } from '../context/SessionContext';
